% Ch. Economic Value (~4-6 pp). Turns the OOS result of Ch.7 into a real-time
% trading strategy for a global bond investor. Exhibits from strategy.R.
\chapter{Economic Value of the Global Cycle Factor}
\label{ch:strategy}

\Cref{sec:res-oos} established the one statistical fact that matters for an
investor: of the cycle factors, only the \emph{global} factor beats the
recursive prevailing mean out of sample. A positive out-of-sample $R^{2}$ is
precisely the condition under which a real-time investor can convert
predictability into utility \citep{campbellthompson2008}. This chapter asks
whether that conversion is economically meaningful by building a fully
out-of-sample market-timing strategy around the global cycle factor and scoring
it against a passive benchmark. The exercise is deliberately self-contained: it
uses only the recursive factor and forecasts of \Cref{sec:meth-oos}, takes no
position that an investor could not have taken in real time, and is implemented
in \texttt{strategy.R}.

% ===========================================================================
\section{Strategy Design}
\label{sec:strat-design}

Three features of the evidence dictate the design. First, the predictability
that survives out of sample is in \emph{local-currency} returns; the dollar
signal is far weaker (\Cref{sec:res-phase3}). The strategy is therefore run for a
\emph{currency-hedged} global investor, with the unhedged US-dollar investor
analysed separately in \Cref{sec:strat-fx}. Second, the global cycle factor is a single
time series, so it carries no information about which country to favour---it
times \emph{aggregate} bond exposure. The investable asset is accordingly a
single GDP-weighted, ten-year global government-bond portfolio, whose one-year
excess return is the weighted average of the national returns. Third, the signal
must be usable in real time, so both the factor and the forecasting regression
respect time~$t$.

Each month the investor forms a real-time forecast of the portfolio's
twelve-month excess return from the recursive regression
$\rx_{t+12}=a+b\,\GCF^{\mathrm{oos}}_{t}+\varepsilon$, estimated only on
outcomes realised by~$t$, and sets a mean--variance exposure
\begin{equation}
  w_{t} \;=\; \frac{1}{\gamma}\,
              \frac{\widehat{\E}_{t}\!\left[\rx_{t+12}\right]}
                   {\widehat{\sigma}^{2}_{t}},
  \label{eq:strat-weight}
\end{equation}
where $\widehat{\sigma}^{2}_{t}$ is the recursive return variance and $\gamma$
the risk aversion. Because both the factor and the regression are recursive, the
exposure is ``doubly out-of-sample''. The benchmark is the same investor using
the recursive prevailing-mean forecast, and a passive buy-and-hold position. The
Sharpe ratio, which is invariant to a constant rescaling of $w_{t}$, is reported
as is; the certainty-equivalent (CER) return is reported at equal average
exposure across strategies so that the variance penalty is comparable.

% ===========================================================================
\section{Performance}
\label{sec:strat-performance}

\Cref{tab:strat-perf} reports the out-of-sample performance over 2005--2024, the
period for which the fully-recursive factor is available. The global bond
portfolio itself has an out-of-sample $R^{2}$ of $0.125$ against the recursive
mean---higher than the pooled panel figure of \Cref{sec:res-oos}, because
aggregating the eleven national cycles into one global signal averages away
estimation noise. That statistical edge translates into economic value. Timing
the portfolio with the global cycle factor raises the annual Sharpe ratio from
$0.31$ for the passive position to $0.39$, and from $0.34$ for an investor who
times on the recursive mean. For a mean--variance investor with risk aversion
$\gamma=5$, the certainty-equivalent gain is $0.58\%$ per year over buy-and-hold
and $0.37\%$ over the mean-timing benchmark; the global cycle factor retains the
highest certainty equivalent at $\gamma=10$ as well.

\begin{table}[t]\centering
  \includegraphics[width=0.9\linewidth]{tables/strat_t1_performance.pdf}
  \caption{Out-of-sample performance of the global-cycle-factor bond-timing
    strategy, 2005--2024. Exposure follows the mean--variance rule
    \eqref{eq:strat-weight} from the recursive $\GCF^{\mathrm{oos}}$ forecast.
    The Sharpe ratio is scale-invariant; mean, volatility, and the
    certainty-equivalent (CER) return are reported at equal average exposure.}
  \label{tab:strat-perf}
\end{table}

\Cref{fig:strat-cumret} shows the cumulative wealth of the timed and passive
portfolios on a non-overlapping annual basis: the timing strategy ends the period
ahead, and most of its outperformance accrues by avoiding the worst of the
bond-market drawdowns rather than by amplifying the good years.
\Cref{fig:strat-exposure} makes the mechanism explicit---the global cycle factor
cuts bond exposure when it signals a low risk premium and restores it when the
signal turns favourable, whereas the mean-timing investor holds a nearly constant
position. The gains are achieved with low turnover, because the global cycle
factor is highly persistent, so realistic transaction costs would erode little of
the advantage.

\begin{figure}[t]\centering
  \includegraphics[width=0.85\linewidth]{figures/strat_f1_cumret.pdf}
  \caption{Cumulative wealth (excess of cash) of the GCF-timed global bond
    portfolio versus buy-and-hold, non-overlapping annual rebalancing at equal
    average exposure.}
  \label{fig:strat-cumret}
\end{figure}

\begin{figure}[t]\centering
  \includegraphics[width=0.85\linewidth]{figures/strat_f2_exposure.pdf}
  \caption{Real-time portfolio exposure (average normalised to one). The
    GCF-timed investor varies bond exposure with the global cycle signal; the
    mean-timing investor is nearly static.}
  \label{fig:strat-exposure}
\end{figure}

% ===========================================================================
\section{The US-Dollar Investor and the FX-Adjusted Factor}
\label{sec:strat-fx}

A US-based investor who holds the global bond portfolio unhedged earns the dollar
excess return \eqref{eq:rx-usd}, which adds the currency return to the local bond
return. The natural signal for this investor is the FX-adjusted global cycle
factor $\FXGCF_{t}$, built in \Cref{sec:fw-fxgcf} to target dollar returns
directly. \Cref{tab:strat-usd} runs the timing rule of \Cref{sec:strat-design}
on the dollar portfolio, using the recursive $\FXGCF^{\mathrm{oos}}$ signal, with
the unadjusted $\GCF^{\mathrm{oos}}$ reported alongside to isolate the value of
the FX adjustment. The sample is shorter (2010--2024) because the FX-adjusted
factor requires a longer recursive burn-in.

\begin{table}[t]\centering
  \includegraphics[width=0.9\linewidth]{tables/strat_t2_usd.pdf}
  \caption{The unhedged US-dollar investor. Timing the dollar excess return
    \eqref{eq:rx-usd} of the 10Y global bond portfolio with the recursive
    FX-adjusted factor $\FXGCF^{\mathrm{oos}}$ and, for comparison, the unadjusted
    $\GCF^{\mathrm{oos}}$. Same mean--variance rule and reporting as
    \Cref{tab:strat-perf}.}
  \label{tab:strat-usd}
\end{table}

Two facts shape the result. First, over this sample the unhedged dollar bond
premium is essentially zero: the passive portfolio earns a slightly negative
average excess return and a Sharpe ratio indistinguishable from zero, because the
strength of the dollar through the 2010s eroded the return on foreign bonds for a
US investor. There is, in other words, little premium for any strategy to harvest.
Second, the FX adjustment does what it was designed to do \emph{statistically}:
the FX-adjusted factor lifts the out-of-sample $R^{2}$ to $0.093$, above the
$0.066$ of the unadjusted global factor, confirming that it carries genuine
incremental information about dollar returns (the in-sample message of
\Cref{sec:res-phase3}, now out of sample).

That statistical edge does not, however, translate into superior risk-adjusted
performance. The FX-adjusted signal is noisier in dollars than in local currency,
so the FXGCF-timed strategy carries a higher volatility and earns a Sharpe ratio
of only $0.03$---below the $0.07$ of the unadjusted GCF-timed strategy and far
below the $0.39$ of the currency-hedged investor in \Cref{sec:strat-performance}.
The conclusion is unambiguous and consistent throughout the thesis: currency
risk, not a failure of the cycle signal, is what caps the dollar investor. The
FX adjustment improves the forecast, but the economic value of the global cycle
factor accrues overwhelmingly to the investor who hedges the currency exposure of
the international bond position.

% ===========================================================================
\section{A Three-Month Illustration}
\label{sec:strat-example}

To make the mechanics concrete, \Cref{tab:strat-example} traces the actions of
the FX-adjusted dollar investor over three consecutive months in mid-2022, an
episode in which the global cycle turned sharply. Each month the investor
performs the same three steps: (i) read the latest value of the recursive
FX-adjusted factor; (ii) update the real-time forecast of the next twelve months'
dollar excess return and the recursive return volatility; and (iii) set the
mean--variance target weight \eqref{eq:strat-weight} and rebalance the portfolio
to it.

\begin{table}[t]\centering
  \includegraphics[width=\linewidth]{tables/strat_t3_example.pdf}
  \caption{Worked example: the FX-adjusted dollar investor over three consecutive
    months. The target weight follows the mean--variance rule
    \eqref{eq:strat-weight}; the realized twelve-month return is shown only to
    judge the call ex post and is not used in forming the weight.}
  \label{tab:strat-example}
\end{table}

In May 2022 the factor still reads slightly positive ($+0.14$); the real-time
model forecasts a $2.96\%$ excess return and, against a recursive volatility of
$5.9\%$, prescribes an overweight position of $1.23$ times the neutral exposure.
The investor holds bonds above benchmark. One month later the factor has fallen
to $-0.16$ and the forecast to $2.06\%$; the target weight drops to $0.81$, so the
investor \emph{sells about a third} of the bond position, moving underweight. By
July the factor has collapsed to $-0.68$, the forecast is a negligible $0.55\%$,
and the rule cuts the weight to $0.20$---the investor \emph{sells three quarters}
of the remaining position and sits almost entirely in cash. The subsequent
realized twelve-month returns, shown in the final column purely to judge the
calls, were $-8\%$ to $-10\%$: the deteriorating global cycle led the investor to
near-cash just before the worst of the 2022 bond drawdown. The episode is also a
fair illustration of the strategy's limits---these large, correct calls coexist
with the modest full-sample Sharpe ratio of \Cref{tab:strat-usd}, because such
decisive turning points are rare.

% ===========================================================================
\section{Caveats}
\label{sec:strat-caveats}

The result should be read as a proof of concept, not a tradable system, and
three limitations bound it. First, the magnitude is modest---a Sharpe-ratio
improvement of roughly $0.08$ and a certainty-equivalent gain below one percent a
year---which is typical of bond-return predictability and consistent with the
small out-of-sample $R^{2}$. Second, the forecasts use overlapping twelve-month
returns, so the supporting precision is overstated relative to non-overlapping
data, and the strategy is evaluated gross of transaction costs (mitigated, but
not eliminated, by the low turnover noted above). Third, because the global cycle
factor is a single series the strategy can only time aggregate exposure; a
cross-sectional strategy would require a factor with country-specific variation,
which the out-of-sample evidence does not support for the local factor. With
these caveats, the conclusion stands: the predictability identified in
\Cref{ch:results} is not merely statistical but carries genuine, if moderate,
economic value for a currency-hedged global bond investor. The broader
interpretation is taken up in \Cref{ch:discussion}.
